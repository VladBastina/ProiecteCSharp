<Window x:Class="Checkers.Views.StatisticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Checkers.Views"
        mc:Ignorable="d"
        Title="StatisticsWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Statistics" FontSize="20" HorizontalAlignment="Center" Margin="0,10"/>
        <StackPanel Grid.Row="1" Margin="10">
            <TextBlock Text="White Wins:"/>
            <TextBlock Text="{Binding WhiteWins}"/>
            <TextBlock Text="Black Wins:"/>
            <TextBlock Text="{Binding BlackWins}"/>
            <TextBlock Text="Maximum Pieces Win:"/>
            <TextBlock Text="{Binding MaxPiecesWin}"/>
        </StackPanel>
        <Button Content="Back" Grid.Row="2" HorizontalAlignment="Center" Click="Button_Click"/>
    </Grid>
</Window>
