<Window x:Class="SuperMarket_Gestiune.Views.UsersAddDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add User" Height="200" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Nume Utilizator:" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBox x:Name="NumeUtilizatorTextBox" Grid.Row="0" Margin="120,0,0,0"/>

        <Label Content="Parola:" Grid.Row="1" VerticalAlignment="Center"/>
        <TextBox x:Name="ParolaTextBox" Grid.Row="1" Margin="120,0,0,0"/>

        <Label Content="Tip Utilizator:" Grid.Row="2" VerticalAlignment="Center"/>
        <TextBox x:Name="TipUtilizatorTextBox" Grid.Row="2" Margin="120,0,0,0"/>

        <Button Content="OK" Width="75" Grid.Row="3" HorizontalAlignment="Right" Margin="0,10,0,0" Click="OK_Click"/>
    </Grid>
</Window>
