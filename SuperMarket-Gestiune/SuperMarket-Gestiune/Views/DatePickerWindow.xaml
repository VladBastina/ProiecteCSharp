<Window x:Class="SuperMarket_Gestiune.Views.DatePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Select a Date" Height="200" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- DatePicker Control -->
        <Label Content="Select a date:" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10"/>
        <DatePicker Name="datePicker" Grid.Row="0" Margin="10"/>

        <!-- OK Button -->
        <Button Content="OK" Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="10" Width="75" Click="OkButton_Click"/>
    </Grid>
</Window>
